import { useWeb3Contract } from "react-moralis"

const LotteryEntrance = () => {
    // const { runContractFunction: enterRaffle} = useWeb3Contract({abi:,contractAddress:,functionName:, params: {}, msgValue: })

    return <div>Hi!</div>
}

export default LotteryEntrance
